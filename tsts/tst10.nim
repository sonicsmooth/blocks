

import rects

# Test whether pointers to rects.Rect and sdl2.Rect yield the same.
# This is interesting because one use int and the other uses cint
# also check for compatability between sdl2.Point and rects.Size
